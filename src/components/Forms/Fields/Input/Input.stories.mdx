import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'

import { Form } from 'components/Forms'
import { ButtonSolid } from 'components/Buttons'
import { PreviewWrapper, StoryWrapper } from 'components/StorybookWrappers'

import { Input } from '.'

<Meta title='Fields/Input' component={Input} />

# Input field

This is a text description :)

<Preview>
	<Story name='All types'>
		<PreviewWrapper>
			<Form form='inputForm' onSubmit={action('submit')}>
				<Input name='text' label='Text' />
				<Input name='number' label='Number' type='number' />
				<Input name='tel' label='Tel' type='tel' />
				<Input name='email' label='Email' type='email' />
				<Input name='password' label='Password' type='password' />
				<Input name='multiline' label='Multiline' type='multiline' />
				<ButtonSolid label='Submit' submit />
			</Form>
		</PreviewWrapper>
	</Story>
</Preview>

### Default

<Story name='Default'>
	<StoryWrapper>
		<Form form='inputForm' onSubmit={action('submit')}>
			<Input name='input' label='Input' />
			<ButtonSolid label='Submit' submit />
		</Form>
	</StoryWrapper>
</Story>

### Required

<Story name='Required'>
	<StoryWrapper>
		<Form form='inputForm' onSubmit={action('submit')}>
			<Input name='input' label='Input' required />
			<ButtonSolid label='Submit' submit />
		</Form>
	</StoryWrapper>
</Story>

### Email

<Story name='Email'>
	<StoryWrapper>
		<Form form='inputForm' onSubmit={action('submit')}>
			<Input name='input' label='Input' type='email' required />
			<ButtonSolid label='Submit' submit />
		</Form>
	</StoryWrapper>
</Story>

### Number

<Story name='Number'>
	<StoryWrapper>
		<Form form='inputForm' onSubmit={action('submit')}>
			<Input name='input' label='Input' type='number' required />
			<ButtonSolid label='Submit' submit />
		</Form>
	</StoryWrapper>
</Story>

### Multiline

<Story name='Multiline'>
	<StoryWrapper>
		<Form form='inputForm' onSubmit={action('submit')}>
			<Input name='input' label='Input' type='multiline' />
			<ButtonSolid label='Submit' submit />
		</Form>
	</StoryWrapper>
</Story>

### Date time

<Story name='Date time'>
	<StoryWrapper>
		<Form form='inputForm' onSubmit={action('submit')}>
			<Input name='input' label='Input' type='time' />
			<ButtonSolid label='Submit' submit />
		</Form>
	</StoryWrapper>
</Story>
